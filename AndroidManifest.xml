<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crazymeal.mobistras"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="com.crazymeal.strasandpark.activities.ParkingListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.crazymeal.strasandpark.activities.AlarmProgrammingActivity"
            android:parentActivityName="com.crazymeal.strasandpark.activities.ParkingListActivity" >
            >
        </activity>

        <service android:name="com.crazymeal.strasandpark.alarms.AlarmService" />

        <receiver 
            android:name="com.crazymeal.strasandpark.alarms.AlarmReceiver" 
            android:enabled="true">
        </receiver>

    </application>

</manifest>